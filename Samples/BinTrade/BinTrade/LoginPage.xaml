<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="BinTrade.LoginPage"
             Title="Binary Trade Demo">
    <StackLayout Orientation="Vertical" VerticalOptions="Center">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>

            <Label Grid.Column="0" Grid.Row="0" Text="API Key" />
            <Entry Grid.Column="1" Grid.Row="0" x:Name="_apiKey" />

            <Label Grid.Column="0" Grid.Row="1" Text="API Secret" />
            <Entry Grid.Column="1" Grid.Row="1" IsPassword="True" x:Name="_apiSecret" />
        </Grid>
        <StackLayout Orientation="Horizontal">
            <Label Text="Save API key and secret" />
            <Switch x:Name="_saveKeySecret" />
        </StackLayout>
        <Button Text="Login" WidthRequest="100" HorizontalOptions="Center" Clicked="OnLoginClicked" />
    </StackLayout>
</ContentPage>